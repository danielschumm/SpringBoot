server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/daniels
    username: daniels
    password: daniels
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
JWT:
  secretkey: "your-very-long-secret-key-that-should-be-kept-safe-and-not-shared"
