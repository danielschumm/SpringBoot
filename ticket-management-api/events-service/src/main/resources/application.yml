server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/daniels
    username: daniels
    password: daniels
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  sql:
    init:
      mode: never
JWT:
  secretkey: "your-very-long-secret-key-that-should-be-kept-safe-and-not-shared"